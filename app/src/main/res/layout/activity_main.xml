<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#071515"
    tools:context=".MainActivity">

    <!-- Title -->
    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Aionia"
        android:textSize="32sp"
        android:textStyle="bold"
        android:textColor="#F2E9C9"
        android:letterSpacing="0.08"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="64dp"/>

    <!-- MAP -->
    <Button
        android:id="@+id/btnMap"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:text="🗺  Map"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#0B1E1E"
        android:backgroundTint="#5E9C7A"
        android:stateListAnimator="@null"
        android:elevation="6dp"
        app:cornerRadius="18dp"
        app:layout_constraintTop_toBottomOf="@id/title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="22dp"/>

    <!-- CALC -->
    <Button
        android:id="@+id/btnCalc"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:text="⚡ Calculator"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#0B1E1E"
        android:backgroundTint="#A7D7B8"
        android:elevation="6dp"
        android:stateListAnimator="@null"
        app:cornerRadius="18dp"
        app:layout_constraintTop_toBottomOf="@id/btnMap"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="22dp"
        android:layout_marginTop="14dp"/>

    <!-- COMPARE -->
    <Button
        android:id="@+id/btnCompare"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:text="📊 Compare"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#0B1E1E"
        android:backgroundTint="#E8C76A"
        android:elevation="6dp"
        android:stateListAnimator="@null"
        app:cornerRadius="18dp"
        app:layout_constraintTop_toBottomOf="@id/btnCalc"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="22dp"
        android:layout_marginTop="14dp"/>

    <!-- ABOUT -->
    <Button
        android:id="@+id/btnInfo"
        android:layout_width="0dp"
        android:layout_height="72dp"
        android:text="ℹ About"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="#0B1E1E"
        android:backgroundTint="#C79A5A"
        android:elevation="6dp"
        android:stateListAnimator="@null"
        app:cornerRadius="18dp"
        app:layout_constraintTop_toBottomOf="@id/btnCompare"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginHorizontal="22dp"
        android:layout_marginTop="14dp"/>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="#0B2222"
        app:menu="@menu/bottom_menu"
        app:itemIconTint="#F2E9C9"
        app:itemTextColor="#F2E9C9"
        app:itemActiveIndicatorStyle="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>